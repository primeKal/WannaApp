<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kalu.wannaapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.PostCommentActivity"></activity>
        <activity android:name=".Activities.VerificationActivity" />
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.lookaround.LookAroundActivity" />
        <activity android:name=".Activities.plugpackage.PluginActivtiy" />
        <activity android:name=".Activities.SignUPActivity" />
        <activity android:name=".Activities.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.exchangepackage.ExchangeActivity" />
    </application>

</manifest>